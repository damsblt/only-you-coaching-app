{
  "name": "only-you-coaching-thumbnail-generator",
  "version": "1.0.0",
  "description": "Lambda function to generate thumbnails from videos uploaded to S3",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.935.0",
    "@neondatabase/serverless": "^0.9.5"
  },
  "devDependencies": {
    "@aws-sdk/client-lambda": "^3.975.0"
  }
}
